# =============================================================================
# Test / CI Dependencies
# Installs the minimal API core plus packages needed for the test suite.
# =============================================================================

# Core API dependencies
-r requirements.minimal.txt

# Additional runtime deps required by the application under test
pydantic-settings==2.1.0
PyJWT==2.8.0
bcrypt==4.1.2
asyncpg==0.29.0
httpx==0.25.2
psutil==5.9.7
aiofiles==23.2.1
python-dotenv==1.0.0

# Test framework
pytest==7.4.3
pytest-asyncio==0.21.1
