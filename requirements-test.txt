# =============================================================================
# Test / CI Dependencies
# Installs the minimal API core plus packages needed for the test suite.
# =============================================================================

# Trusted release runtime dependencies
-r requirements.release.txt

# Test framework
pytest==7.4.3
pytest-asyncio==0.21.1

# Load testing (M7)
locust==2.24.0

# Test-only helpers
types-requests==2.33.0.20260327
