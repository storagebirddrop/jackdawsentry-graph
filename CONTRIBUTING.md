# Contributing

## Before Opening a PR

- run the graph backend tests
- run `npm run lint` and `npm run build` in `frontend/app`
- keep changes scoped to the standalone graph product
- avoid introducing private-platform assumptions or compliance-only language

## Review Expectations

- preserve `ExpansionResponse v2`
- keep docs public-safe
- prefer focused changes over broad mixed refactors
