# =============================================================================
# Docker Runtime Dependencies
# Core API + all runtime deps needed by mounted routers and engines.
# =============================================================================

# Core API dependencies
-r requirements.minimal.txt

# Additional runtime deps required by the application
pydantic-settings==2.1.0
PyJWT==2.8.0
bcrypt==4.1.2
asyncpg==0.29.0
httpx==0.25.2
psutil==5.9.7
aiofiles==23.2.1
python-dotenv==1.0.0

# Data processing (compliance analytics, export, visualization engines)
pandas>=2.1.0
numpy>=1.26.0
matplotlib>=3.8.0

# Async HTTP (intelligence, collectors)
aiohttp>=3.9.0

# Prometheus monitoring
prometheus-client>=0.19.0

# Scheduling (compliance scheduler)
schedule>=1.2.0

# gRPC (Lightning Network collector)
grpcio>=1.60.0

# XML parsing (sanctions OFAC ingestion)
defusedxml==0.7.1

# PDF export (investigation reports, compliance exports)
reportlab>=4.0.0

# Excel export (compliance exports)
openpyxl>=3.1.0

# AI narrative generation (optional — falls back to template if key absent)
anthropic>=0.25.0

# scikit-learn (ML risk model feature extraction)
scikit-learn>=1.4.0

# Blockchain SDKs (Ethereum RPC + EVM chains)
web3>=6.15.0
eth-abi>=4.2.0
