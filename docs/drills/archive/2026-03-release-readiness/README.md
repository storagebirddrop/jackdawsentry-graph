# 2026-03 Release Readiness Archive

This archive preserves the raw phase, wave, release-gate, and browser-proof
records produced during the 2026-03 release-readiness drill.

Contents:

- `records/` contains the generated markdown packets and handoff files
- `artifacts/browser/` contains the captured browser screenshots and matching
  JSON payload evidence

These files are preserved as generated execution records. Some of them still
contain capture-time absolute paths or references to the original root-level
filenames; treat those as historical context, not as the preferred structure
for future runs.
